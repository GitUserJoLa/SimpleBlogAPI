spring:
  application:
    name: simpleBlogApp
  datasource:
    url: jdbc:postgresql://localhost:5432/blogdata
    username: josephinelanger
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql=true:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect